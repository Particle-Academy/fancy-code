// `highlightCode` now lives in @particle-academy/fancy-file-commons (shared
// pure core). Re-exported so existing `../engine/highlight` imports are stable.
export { highlightCode } from "@particle-academy/fancy-file-commons";
